name: Backend Trips Tests

on:
  push:
    paths:
      - "backend/apps/trips/**"
      - "backend/apps/users/**"
      - "backend/config/**"
      - "backend/requirements.txt"
      - ".github/workflows/backend-trips.yml"
  pull_request:
    paths:
      - "backend/apps/trips/**"
      - "backend/apps/users/**"
      - "backend/config/**"
      - "backend/requirements.txt"
      - ".github/workflows/backend-trips.yml"

jobs:
  trips-tests:
    name: Trips tests
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Apply migrations
        run: |
          python manage.py migrate --no-input

      - name: Run trips tests
        run: python manage.py test apps.trips --verbosity=2 --failfast

      - name: Django system check
        run: python manage.py check
